[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Resilphase-FLUX-Diffusers"
version = "0.1.0"
description = "ResilPhase-FLUX acceleration patch for HuggingFace Diffusers FLUX pipelines"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "torch",
  "diffusers>=0.35.0",
]

[tool.setuptools.packages.find]
where = ["src"]
